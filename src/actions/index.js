const Reflux = require('reflux');

const KillopActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'toggleStatus'
]);

module.exports = KillopActions;
